{
  "name": "toktumi",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "precommit": "lerna run --concurrency 1 --stream precommit"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.5.1"
  }
}
