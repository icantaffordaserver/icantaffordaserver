type SignupEmailUserPayload {
  id: ID!
}

extend type Mutation {
  signUpUser(
    email: String!
    password: String!
    firstName: String!
    lastName: String!
    birthday: String!
    bio: String!
    inviteId: ID
  ): SignupEmailUserPayload
}
