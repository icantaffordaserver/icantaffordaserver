subscription {
  Invites(
    filter: { mutation_in: [UPDATED], updatedFields_contains: "isApproved" }
  ) {
    updatedFields
    node {
      firstName
      lastName
      emailToInvite
      isApproved
    }
  }
}
