{
  "name": "typeformWebhook",
  "version": "0.0.1",
  "description": "Lambda function to handle typeform webhooks from zapier when a user submits their profile on toktumi",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "transpile": "babel src -d lib",
    "build": "npm run clean && npm run transpile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexander Mann",
  "license": "UNLICENSED",
  "dependencies": {
    "axios": "^0.16.1",
    "babel-polyfill": "^6.23.0",
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-babili": "^0.0.12",
    "babel-preset-env": "^1.4.0",
    "babel-preset-stage-0": "^6.24.1",
    "rimraf": "^2.6.1"
  }
}
