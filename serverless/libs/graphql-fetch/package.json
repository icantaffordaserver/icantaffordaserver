{
  "name": "graphql-fetch",
  "version": "0.0.1",
  "description": "A simple client using node-fetch to interact with a graphql api",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "build": "npm run clean && babel src --out-dir lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build"
  },
  "author": "Alexander Mann",
  "license": "UNLICENSED",
  "dependencies": {
    "node-fetch": "^1.6.3"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.24.1",
    "babel-preset-env": "^1.4.0",
    "babel-preset-stage-3": "^6.24.1",
    "rimraf": "^2.6.1"
  }
}
