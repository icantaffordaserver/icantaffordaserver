{
  "name": "mailer",
  "version": "1.0.0",
  "description": "A mail client using postmark to send toktumi related emails",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "build": "npm run clean && babel src --out-dir lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build"
  },
  "author": "Alexander Mann",
  "license": "ISC",
  "dependencies": {
    "postmark": "^1.3.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.24.1",
    "babel-preset-env": "^1.4.0",
    "babel-preset-stage-3": "^6.24.1",
    "rimraf": "^2.6.1"
  }
}
